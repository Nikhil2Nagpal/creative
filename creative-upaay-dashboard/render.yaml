services:
  - type: web
    name: creative-upaay
    env: node
    buildCommand: |
      echo "Current directory: $(pwd)"
      ls -la
      npm install
      npm run build
    startCommand: npx serve -s dist