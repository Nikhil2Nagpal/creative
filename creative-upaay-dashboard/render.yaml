services:
  - type: web
    name: creative-upaay-dashboard
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s dist
    envVars:
      - key: NODE_ENV
        value: production